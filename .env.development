# ===========================================
# 开发环境配置 (Development Environment)
# ===========================================
# 作用：执行 `pnpm dev` 时加载此文件
# 说明：开发环境直接访问黑马公开 API（存在跨域，由 Vite dev server 代理或浏览器 CORS 处理）

# API 基础地址（后端服务地址）
# 注意：必须使用 https://，后端已强制 HTTP→HTTPS 重定向，浏览器预检请求不允许重定向
VITE_API_BASE_URL=https://big-event-vue-api-t.itheima.net

# 应用部署基础路径（默认根路径，部署到子路径时改为如 '/admin/'）
VITE_BASE_PATH=/
