# ===========================================
# 生产环境配置 (Production Environment)
# ===========================================
# 作用：执行 `pnpm build` 时加载此文件
# 说明：替换为你的实际后端 API 地址。本地开发用 pnpm mock + pnpm dev

# API 基础地址（部署时改为你的后端地址，或留空走同源 /api /my）
VITE_API_BASE_URL=

# 应用部署基础路径（默认根路径，部署到子路径时改为如 '/admin/'）
VITE_BASE_PATH=/
