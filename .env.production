# ===========================================
# 生产环境配置 (Production Environment)
# ===========================================
# 作用：执行 `pnpm build` 时加载此文件
# 说明：当前仍指向公开 API；若自建后端或使用 Nginx 反向代理，请改为同源地址（如留空走相对路径 '/api'）

# API 基础地址（后端服务地址）
# 注意：必须使用 https://，后端已强制 HTTP→HTTPS 重定向
VITE_API_BASE_URL=https://big-event-vue-api-t.itheima.net

# 应用部署基础路径（默认根路径，部署到子路径时改为如 '/admin/'）
VITE_BASE_PATH=/
